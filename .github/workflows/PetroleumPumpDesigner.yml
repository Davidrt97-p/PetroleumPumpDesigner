name: Build Windows Executable # Puedes mantener el nombre o cambiarlo a "Debug Workspace"
on:
  push:
    branches: [ main ] 
  workflow_dispatch: 

jobs:
  build: # Puedes renombrar el job a 'debug_ubuntu_workspace' si quieres
    runs-on: ubuntu-latest # <--- ¡CAMBIO IMPORTANTE AQUÍ!

    steps:
    # Paso 1: Solo checkout, sin path, para ver dónde realmente pone las cosas
    - name: Checkout code (default behavior)
      uses: actions/checkout@v4

    # Paso 2: Listar TODO desde GITHUB_WORKSPACE para entender la estructura
    - name: Debug Initial Directory Structure
      shell: bash # En Ubuntu, bash es el shell por defecto, pero no hace daño ser explícito
      run: |
        echo "GITHUB_WORKSPACE: ${{ github.workspace }}"
        echo "Initial PWD: $(pwd)"
        echo "------------------------------------"
        echo "Content of GITHUB_WORKSPACE (RECURSIVE ls -R):"
        ls -R ${{ github.workspace }} || echo "ls -R failed for GITHUB_WORKSPACE"

    # El resto de los pasos siguen comentados porque solo queremos depurar el workspace y el checkout
    # - name: Set up Node.js
    #   uses: actions/setup-node@v4
    #   with:
    #     node-version: '18' 
    # ... (y así sucesivamente)
